@model List<Question>
@using Microsoft.AspNetCore.Identity
@inject UserManager<IdentityUser> UserManager

<hr />

    <div class="row">
        <div class="col-2 border-right">Submitter</div>
        <div class="col-2 border-right">Question</div>
        <div class="col-3">Format</div>
    </div>
    <br />
@foreach(var question in Model)
{
    <div class="row border-top">
        @Html.HiddenFor(q => question.QuestionID)
        <div class="col-2">
        @Html.DisplayFor(q => question.SubmitterID) 
        </div>
        <div class="col-2 border-right">
            @question.Text
        </div>
        <div class="col-3">
            @switch(question.Format)
            {
                case Format.MultipleChoice: <span>Multiple Choice</span>
                break;
                case Format.TrueFalse: <span>True/False</span>
                break;
                case Format.ShortAnswer: <span>Short Answer</span>
                break;
                default: <span> Invalid/Format</span>
                break;
            }
        </div>
        <button id="showAnswers" aria-expanded="false" aria-controls=a-@question.QuestionID data-toggle="collapse" data-target=#a-@question.QuestionID class="btn btn-secondary btn-sm">
             Show/Hide answers</button> 
            <div class="col collapse" id=a-@question.QuestionID>
                <div class="col">
                @foreach(var answer in question.Answers)
                {
                    <div class="row">
                        <div class="col-1">
                            @if(answer.Correct)
                            {
                                <span style="color:Green">&check;</span>
                            }else
                            {
                                <span style="color:Red">&cross;</span>
                            }
                        </div>
                        <div class="col">
                            @answer.Text
                        </div>
                    </div>
                }
                </div>
            </div>
        <div class="col">
          <button type="button" class="btn btn-danger btn-sm float-right" data-toggle="modal" data-target="#m-@question.QuestionID">
          Delete
          </button> 
  
            <div class="modal fade" id="m-@question.QuestionID" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
              <div class="modal-dialog modal-dialog-centered" role="document">
                <div class="modal-content">
                  <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLongTitle">Delete</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                      <span aria-hidden="true">&times;</span>
                    </button>
                  </div>
                  <div class="modal-body">
                    Are you sure you want to delete this quesion?
                  </div>
                  <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Nevermind</button>
                    @Html.ActionLink("Delete", "Delete", new { questionId=@question.QuestionID}, new {@class="btn btn-danger" })
                  </div>
                </div>
              </div>
            </div> 
        </div>
    </div>
}
<!--
@Html.ActionLink("Create", "Create", new { userID=@UserManager.GetUserId(User)}, new { @class="btn btn-primary mt-4" })
-->